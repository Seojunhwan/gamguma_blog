datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
}

model PostViews {
  id        Int      @id @default(autoincrement())
  slug      String   @unique
  views     Int      @default(0)
}